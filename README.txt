This is a Python project.

Tree is already built on ./rules/rules.py. Based on that, on this folder you can find a .png
with the tree.

If you want to test the program by yourself, then you will need chefboost, to install it:

python -m pip install chefboost (on Windows, or equivalent command
in other OS)

Run:

python restaurant.py